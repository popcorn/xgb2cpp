#ifndef XGBOOST_REGRESSOR_H  // To make sure you don't declare the function more than once by including the header multiple times.
#define XGBOOST_REGRESSOR_H

#include <vector>

float xgb_regress(std::vector<float> &sample);

#endif
